apiVersion: v1
kind: Service
metadata:
  name: {{ include "vijju-chart.fullname" . }}
  labels:
    {{- include "vijju-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vijju-chart.selectorLabels" . | nindent 4 }}
